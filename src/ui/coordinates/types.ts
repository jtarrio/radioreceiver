/** Represents an unzoomed position on a visual element. Range is [0, 1). */
export type WidthFraction = number;
/** Represents a zoomable position on a visual element. Range is [0, 1). */
export type DisplayFraction = number;
